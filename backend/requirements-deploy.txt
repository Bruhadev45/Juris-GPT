# Production deployment requirements — slim image (~300MB vs ~6GB).
#
# The deployed configuration is hybrid BM25 retrieval + Claude generation
# (see the five-config benchmark in data/eval/results): none of the local-LLM
# or neural-retrieval stacks (torch, transformers, sentence-transformers,
# llama-cpp-python, datasets) are imported on this path — rag_pipeline.py
# lazy-imports them only when the reranker/dense store is enabled.
# Use backend/requirements.txt for local research work.

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.8.0
pydantic-settings>=2.2.0
aiofiles>=23.0.0
supabase>=2.3.0
openai>=1.12.0
python-docx>=1.1.0
reportlab>=4.1.0
razorpay>=1.4.1
resend>=0.7.0
httpx>=0.26.0
python-multipart>=0.0.6
PyPDF2>=3.0.0
markdown>=3.5.0
email-validator>=2.0.0
boto3>=1.34.0
bcrypt>=4.0.0
sse-starlette>=1.6.0
requests>=2.31.0

# Hybrid retrieval (BM25 + inverted index)
rank-bm25>=0.2.2

# Anthropic Claude (primary generation model)
anthropic>=0.40.0
langchain-anthropic>=0.3.0
