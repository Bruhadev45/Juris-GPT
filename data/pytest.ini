[pytest]
testpaths = eval
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    unit: fast, in-process unit tests
    integration: tests that exercise multiple modules but no external services
